import numpy as np
from scipy import special

# numpy
degrees = np.degrees
radians = np.radians

cos = np.cos
sin = np.sin
arcsin = np.arcsin
arccos = np.arccos
arctan2 = np.arctan2
pi = np.pi

isnan = np.isnan
power = np.power
matmul = np.matmul
multiply = np.multiply
inner = np.inner
arange = np.arange
abs = np.abs
ceil = np.ceil
where = np.where

logical_xor = np.logical_xor

# cipy
sph_harm = special.sph_harm
lpmv = special.lpmv
